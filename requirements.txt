# Environment used for the reported runs:
#   Python 3.12.12
#   PyTorch 2.10.0+cu128 on CUDA 12.8 / H100 GPUs
#
# FlashAttention-3 is loaded at runtime through kernels.get_kernel("varunneal/flash-attention-3")

torch==2.10.0
triton==3.6.0
kernels==0.12.2
tiktoken==0.12.0
wandb==0.25.1

numpy==2.4.3
datasets==4.7.0
huggingface-hub==1.6.0
tqdm==4.67.3
